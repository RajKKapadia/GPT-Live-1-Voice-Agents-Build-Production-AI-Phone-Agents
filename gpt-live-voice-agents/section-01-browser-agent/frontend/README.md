# Section 1: Browser Agent — Frontend

Requires Bun and the [backend](../backend/README.md) running. In another terminal, run from this `frontend` directory:

```bash
bun install
bun run dev
```

Open the URL printed by Vite (usually http://localhost:5173), click **Start Conversation**, and allow microphone access.
